language: php
php:
    - 5.6
    - 5.5
    - 5.4
    - hhvm

sudo: false

before_script:
    - composer install
    - mkdir -p build/logs

script:
    - phpunit

after_script:
    - vendor/bin/coveralls
